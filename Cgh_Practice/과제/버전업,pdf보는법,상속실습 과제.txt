# 학생관리 프로그램 버전업
	1.  학생 저장시 중복학생은 저장이 안되는 기능추가
	    중복기준 : 이름, 나이, 성별, 학년, 반
	2. 차후 학생뿐만 아니라 강사, 사원을 관리하는 기능을 추가할 예정으로
	   나중에 변경을 잘 할 수 있게 코드를 수정할 것.


# 객체 과제 pdf 보는 법
	+ : public
	- : private
	# : protected
	두껍게 밑줄은 static final
	
	위에는 field
	아래는 생성자/메소드
	
	... 화살표 구현 관계, implements 해라~
	실선 화살표 이용관계 화살표 받는 클래스에서 화살표 쏜 클래스 내용을 이용한다.


# 프로젝트 생성 후 실습!
package com.bs.practice.inherit.controller;

import com.bs.practice.inherit.vo.Driver;
import com.bs.practice.inherit.vo.Employee;
import com.bs.practice.inherit.vo.Person;
import com.bs.practice.inherit.vo.Student;
import com.bs.practice.inherit.vo.Teacher;

	// 사람들의 정보를 저장하는 프로그램을 생성
	// 학생, 선생, 직원, 운전사를 아래의 데이터를 저장하고 관리
	// 학생 : 이름, 나이, 성별, 수강과목, 반
	// 선생 : 이름, 나이, 성별, 전공과목, 담당과목
	// 직원 : 이름, 나이, 성별, 부서, 전화번호
	// 운전 : 이름, 나이, 성별, 담당지역, 운전경력
	// 효율적인 클래스를 설계하고 만들어보자 * 상속관계
	// 각 객체를 한개씩 생성해서 내용 출력
	// 객체 동등성비교 하기
	// 학생 : 이름 나이 성별 반이 같으면 같은 객체
	// 선생 : 이름 나이 전공과목이 같으면 같은 객체
	// 직원 : 이름 나이 부서 가 같으면 같은 객체
	// 운전 : 이름 나이 성별 담당지역 운전경력이 같으면 같은 객체
	// 모두 변수로 필드데이터를 모두 출력할 수 있게 설정
	// 성별은 '남','여' 만 입력이 가능하게 수정하고
	// age는 10이상만 가능, 이름은 두글자 이상만 가능 버전업!